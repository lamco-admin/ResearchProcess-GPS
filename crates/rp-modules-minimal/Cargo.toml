[package]
name = "rp-modules-minimal"
version = "0.1.0"
edition = "2021"

[dependencies]
# Just the bare minimum
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = "1"
tokio = { version = "1", features = ["sync"] }
thiserror = "1"

# NO wasmtime or heavy dependencies